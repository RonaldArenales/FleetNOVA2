# Copy this file to .env and edit DATABASE_URL with your local postgres
# superuser password (find it out with `psql -U postgres -h localhost`,
# or ask whoever installed PostgreSQL on this machine).
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/fleetnova?schema=public"
JWT_SECRET=changeme
DEVICE_API_KEY=changeme-device-key
PORT=4000
